# خطوات التكامل للواجهة الأمامية - Frontend Integration Steps

## ✅ التحديثات المكتملة في Backend

تم تحديث Backend بنجاح لدعم جميع أنواع الاستعجال المطلوبة. الـ API جاهز الآن للتكامل مع الواجهة الأمامية.

---

## 📋 الخطوات المطلوبة للواجهة الأمامية

### 1. تحديث واجهة "الوظائف المستعجلة"

#### 1.1 إضافة النوع الجديد "عقود معقدة"

في ملف الترجمات/اللغات (`LanguageContext` أو ما شابه):

```javascript
// العربية
urgent_opt_now: "مطلوب الآن"
urgent_opt_temp: "عقود مؤقتة"
urgent_opt_daily: "دفع يومي"
urgent_opt_complex: "عقود معقدة"  // ⬅️ جديد

// الإنجليزية
urgent_opt_now: "Needed Now"
urgent_opt_temp: "Temp Contracts"
urgent_opt_daily: "Daily Payment"
urgent_opt_complex: "Complex Contracts"  // ⬅️ جديد
```

#### 1.2 تحديث أزرار الفلترة في UrgentJobsView

```jsx
const specialTags = [
  { id: 'مطلوب الآن', labelKey: 'urgent_opt_now', icon: '🔴' },
  { id: 'عقود مؤقتة', labelKey: 'urgent_opt_temp', icon: '📋' },
  { id: 'دفع يومي', labelKey: 'urgent_opt_daily', icon: '💰' },
  { id: 'عقود معقدة', labelKey: 'urgent_opt_complex', icon: '📑' }  // ⬅️ جديد
];
```

---

### 2. تحديث CreatePostModal

#### 2.1 إضافة خيار "عقود معقدة"

عند اختيار `displayPage="urgent"` في نموذج إنشاء المنشور:

```jsx
{displayPage === 'urgent' && (
  <div className="special-tag-selector">
    <label>نوع الاستعجال *</label>
    <select 
      value={specialTag} 
      onChange={(e) => setSpecialTag(e.target.value)}
      required
    >
      <option value="">اختر النوع...</option>
      <option value="مطلوب الآن">مطلوب الآن</option>
      <option value="عقود مؤقتة">عقود مؤقتة</option>
      <option value="دفع يومي">دفع يومي</option>
      <option value="عقود معقدة">عقود معقدة</option> {/* ⬅️ جديد */}
    </select>
  </div>
)}
```

#### 2.2 التحقق من الصحة

تأكد من أن `specialTag` إلزامي عند اختيار `displayPage='urgent'`:

```javascript
const validatePost = () => {
  if (displayPage === 'urgent' && !specialTag) {
    setError('يجب اختيار نوع الاستعجال عند النشر في الصفحة المستعجلة');
    return false;
  }
  return true;
};
```

---

### 3. تحديث استدعاءات API

#### 3.1 جلب الوظائف المستعجلة

```javascript
// في UrgentJobsView.jsx أو ما شابه
const fetchUrgentJobs = async (filterTag = null) => {
  try {
    setLoading(true);
    
    const url = filterTag
      ? `/api/v1/posts?displayPage=urgent&specialTag=${encodeURIComponent(filterTag)}`
      : '/api/v1/posts?displayPage=urgent';
    
    const response = await fetch(url, {
      headers: {
        'Authorization': `Bearer ${token}`
      }
    });
    
    const data = await response.json();
    
    if (data.success) {
      setPosts(data.posts);
    } else {
      setError(data.message);
    }
  } catch (error) {
    console.error('Error fetching urgent jobs:', error);
    setError('حدث خطأ أثناء جلب البيانات');
  } finally {
    setLoading(false);
  }
};
```

#### 3.2 إنشاء منشور مستعجل

```javascript
// في CreatePostModal.jsx أو ما شابه
const createUrgentPost = async () => {
  if (!validatePost()) return;
  
  try {
    setSubmitting(true);
    
    const response = await fetch('/api/v1/posts', {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${token}`,
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        content: postContent,
        displayPage: 'urgent',
        specialTag: specialTag,  // إلزامي!
        category: category,
        scope: scope,
        country: country,
        city: city,
        contactPhone: phone,
        contactMethods: contactMethods
      })
    });
    
    const data = await response.json();
    
    if (data.success) {
      showSuccess('تم نشر المنشور بنجاح');
      onPostCreated(data.post);
    } else {
      showError(data.message);
    }
  } catch (error) {
    console.error('Error creating post:', error);
    showError('حدث خطأ أثناء النشر');
  } finally {
    setSubmitting(false);
  }
};
```

---

### 4. معالجة الأخطاء

#### 4.1 رسائل الخطأ من Backend

Backend يرسل رسائل خطأ بالعربية. تأكد من عرضها بشكل صحيح:

```javascript
// إذا لم يتم اختيار specialTag
{
  "success": false,
  "message": "يجب اختيار نوع الاستعجال عند النشر في الصفحة المستعجلة"
}

// إذا كانت قيمة specialTag غير صحيحة
{
  "success": false,
  "message": "Post validation failed: specialTag: `قيمة_خاطئة` is not a valid enum value..."
}
```

#### 4.2 عرض رسائل الخطأ

```jsx
{error && (
  <div className="error-message">
    {error}
  </div>
)}
```

---

### 5. اختبار التكامل

#### 5.1 اختبار الفلترة

1. افتح صفحة الوظائف المستعجلة
2. اضغط على "مطلوب الآن" - يجب أن تظهر المنشورات المناسبة فقط
3. اضغط على "عقود مؤقتة" - يجب أن تظهر المنشورات المناسبة فقط
4. اضغط على "دفع يومي" - يجب أن تظهر المنشورات المناسبة فقط
5. اضغط على "عقود معقدة" (الجديد) - يجب أن تظهر المنشورات المناسبة فقط
6. اضغط على الزر مرة أخرى - يجب أن تظهر جميع المنشورات

#### 5.2 اختبار إنشاء منشور

1. افتح نموذج إنشاء منشور
2. اختر "الصفحة المستعجلة" في نطاق النشر
3. يجب أن يظهر حقل اختيار نوع الاستعجال (required)
4. اكتب محتوى وحاول النشر بدون اختيار النوع - يجب أن تظهر رسالة خطأ
5. اختر نوع (مثل "عقود معقدة")
6. انشر المنشور - يجب أن ينجح

#### 5.3 اختبار عرض المنشورات

1. تحقق من أن المنشورات المستعجلة لا تظهر في الصفحة الرئيسية
2. تحقق من أن المنشورات المستعجلة تظهر فقط في صفحة الوظائف المستعجلة
3. تحقق من أن badge النوع يظهر بشكل صحيح على كل منشور

---

### 6. معلومات إضافية

#### 6.1 القيم الصحيحة لـ specialTag

⚠️ **مهم:** استخدم القيم بالعربية بالضبط:

```javascript
const VALID_SPECIAL_TAGS = [
  'مطلوب الآن',
  'عقود مؤقتة',
  'دفع يومي',
  'عقود معقدة'
];
```

#### 6.2 تشفير النص في URL

عند إرسال النص العربي في URL، استخدم `encodeURIComponent()`:

```javascript
const filterUrl = `/api/v1/posts?displayPage=urgent&specialTag=${encodeURIComponent('مطلوب الآن')}`;
```

#### 6.3 التوافق مع المنشورات القديمة

المنشورات القديمة (قبل هذا التحديث) لها `specialTag: null`. تعامل معها بشكل صحيح:

```javascript
const getSpecialTagLabel = (tag) => {
  if (!tag) return null;
  
  switch(tag) {
    case 'مطلوب الآن': return t('urgent_opt_now');
    case 'عقود مؤقتة': return t('urgent_opt_temp');
    case 'دفع يومي': return t('urgent_opt_daily');
    case 'عقود معقدة': return t('urgent_opt_complex');
    default: return tag;
  }
};
```

---

## 📚 مراجع مفيدة

- **API Guide الكامل:** انظر `URGENT_JOBS_API_GUIDE.md`
- **تفاصيل الميزة:** انظر `URGENT_JOBS_UPDATE.md`

---

## 🐛 المشاكل الشائعة وحلولها

### المشكلة 1: "يجب اختيار نوع الاستعجال"
**السبب:** لم يتم إرسال `specialTag` مع `displayPage='urgent'`
**الحل:** تأكد من إضافة `specialTag` في body الطلب

### المشكلة 2: الفلترة لا تعمل
**السبب:** قيمة `specialTag` غير صحيحة أو غير مشفرة
**الحل:** 
- استخدم القيمة الصحيحة بالعربية
- استخدم `encodeURIComponent()` في URL

### المشكلة 3: المنشورات المستعجلة تظهر في الصفحة الرئيسية
**السبب:** عدم استخدام `displayPage=urgent` في الطلب
**الحل:** تأكد من إضافة `?displayPage=urgent` في URL

---

## ✅ Checklist للواجهة الأمامية

- [ ] إضافة "عقود معقدة" في الترجمات
- [ ] تحديث أزرار الفلترة في UrgentJobsView
- [ ] إضافة خيار "عقود معقدة" في CreatePostModal
- [ ] إضافة التحقق من specialTag عند displayPage='urgent'
- [ ] تحديث دالة جلب البيانات لدعم الفلترة
- [ ] تحديث دالة إنشاء المنشور لإرسال specialTag
- [ ] إضافة معالجة لرسائل الخطأ
- [ ] اختبار الفلترة لجميع الأنواع الأربعة
- [ ] اختبار إنشاء منشور مستعجل
- [ ] اختبار عرض المنشورات في الصفحات المختلفة

---

## 📞 الدعم

إذا واجهت أي مشاكل أثناء التكامل، راجع:
1. `URGENT_JOBS_API_GUIDE.md` للتفاصيل الكاملة
2. Network tab في Developer Tools للتحقق من الطلبات والاستجابات
3. Console للتحقق من الأخطاء

**التكامل بسيط ومباشر!** 🚀
