# مهنتي لي - Backend API

واجهة برمجية (API) لتطبيق مهنتي لي - منصة للإعلانات والوظائف والخدمات.

## 🚀 المميزات

- ✅ نظام المصادقة (تسجيل/دخول) مع JWT
- ✅ إدارة المنشورات الإعلانية
- ✅ التصنيفات (وظائف، حراج، خدمات)
- ✅ نطاق النشر (عالمي/محلي مع اختيار الدولة والمدينة)
- ✅ معلومات التواصل (بريد إلكتروني، هاتف)
- ✅ تمييز الإعلانات (Featured)
- ✅ القصص (Stories) - تنتهي بعد 24 ساعة
- ✅ الفيديوهات القصيرة (Shorts)
- ✅ نظام المتابعة
- ✅ الإشعارات
- ✅ نظام البلاغات
- ✅ رفع الملفات (صور/فيديوهات)

## 📦 التقنيات المستخدمة

- **Node.js** - بيئة التشغيل
- **Express.js** - إطار العمل
- **MongoDB** - قاعدة البيانات
- **Mongoose** - ODM للتعامل مع MongoDB
- **JWT** - للمصادقة
- **Multer** - لرفع الملفات
- **bcryptjs** - لتشفير كلمات المرور

## 🛠️ التثبيت

### 1. استنساخ المستودع
```bash
git clone https://github.com/YOUR_USERNAME/mehnati-backend.git
cd mehnati-backend
```

### 2. تثبيت الحزم
```bash
npm install
```

### 3. إعداد متغيرات البيئة
```bash
cp .env.example .env
```

ثم قم بتعديل ملف `.env` وأضف:
- رابط اتصال MongoDB Atlas
- مفتاح JWT السري

### 4. تشغيل الخادم

للتطوير:
```bash
npm run dev
```

للإنتاج:
```bash
npm start
```

## 📚 API Endpoints

### المصادقة (Auth)
| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/v1/auth/register` | تسجيل مستخدم جديد |
| POST | `/api/v1/auth/login` | تسجيل الدخول |
| GET | `/api/v1/auth/me` | الحصول على بيانات المستخدم الحالي |

### المنشورات (Posts)
| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/v1/posts` | جلب جميع المنشورات |
| GET | `/api/v1/posts/:id` | جلب منشور محدد |
| POST | `/api/v1/posts` | إنشاء منشور جديد |
| PUT | `/api/v1/posts/:id` | تحديث منشور |
| DELETE | `/api/v1/posts/:id` | حذف منشور |
| POST | `/api/v1/posts/:id/react` | إعجاب/إلغاء إعجاب |
| POST | `/api/v1/posts/:id/comments` | إضافة تعليق |

### الفيديوهات القصيرة (Shorts)
| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/v1/posts/shorts/for-you` | فيديوهات لك |
| GET | `/api/v1/posts/shorts/friends` | فيديوهات الأصدقاء |

### المستخدمين (Users)
| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/v1/users/me` | الملف الشخصي |
| PUT | `/api/v1/users/me` | تحديث الملف الشخصي |
| GET | `/api/v1/users/:id` | ملف مستخدم آخر |
| GET | `/api/v1/users/suggested` | المستخدمين المقترحين |

### المتابعة (Follow)
| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/v1/follow/:id` | متابعة/إلغاء متابعة |
| GET | `/api/v1/follow/:id/status` | حالة المتابعة |

### القصص (Stories)
| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/v1/stories` | إنشاء قصة |
| GET | `/api/v1/stories/feed` | قصص المتابَعين |
| POST | `/api/v1/stories/:id/view` | مشاهدة قصة |

### البلاغات (Reports)
| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/v1/reports` | إرسال بلاغ |

## 📁 هيكل المشروع

```
mehnati-backend/
├── src/
│   ├── config/
│   │   └── database.js
│   ├── controllers/
│   │   ├── authController.js
│   │   ├── postController.js
│   │   ├── userController.js
│   │   ├── storyController.js
│   │   └── reportController.js
│   ├── middleware/
│   │   ├── auth.js
│   │   ├── upload.js
│   │   └── errorHandler.js
│   ├── models/
│   │   ├── User.js
│   │   ├── Post.js
│   │   ├── Story.js
│   │   ├── Notification.js
│   │   └── Report.js
│   ├── routes/
│   │   ├── auth.js
│   │   ├── posts.js
│   │   ├── users.js
│   │   ├── follow.js
│   │   ├── stories.js
│   │   └── reports.js
│   └── server.js
├── uploads/
├── .env.example
├── .gitignore
├── package.json
└── README.md
```

## 🔒 الأمان

- تشفير كلمات المرور باستخدام bcrypt
- مصادقة JWT
- حماية CORS
- Helmet للحماية من الثغرات الشائعة

## 📝 ملاحظات

- تأكد من إضافة عنوان IP الخاص بالخادم في MongoDB Atlas Network Access
- في الإنتاج، قم بتغيير `JWT_SECRET` لقيمة آمنة
- قم بتحديد النطاقات المسموح بها في CORS

## 📄 الرخصة

ISC
