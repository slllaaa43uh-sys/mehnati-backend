# تحديث صفحة الوظائف المستعجلة - Urgent Jobs Feature

## 📅 تاريخ التحديث
17 يناير 2026

---

## 🎯 الهدف من التحديث

إضافة صفحة جديدة للوظائف المستعجلة (Urgent Jobs) مع نظام فلترة متقدم يسمح للمستخدمين بنشر وعرض الفرص الفورية والمؤقتة.

---

## ✨ الميزات الجديدة

### 1. **صفحة الوظائف المستعجلة**
- صفحة مخصصة لعرض الفرص الفورية فقط
- نظام فلترة بثلاثة أنواع من الاستعجال
- عرض جميع المنشورات المستعجلة في البداية

### 2. **أنواع الاستعجال (Urgency Types)**

| النوع | الوصف |
|------|-------|
| 🔴 **مطلوب الآن** | وظائف تحتاج تعيين فوري |
| 📋 **عقود مؤقتة** | وظائف بعقود محددة المدة |
| 💰 **دفع يومي** | وظائف بدفع يومي أو أسبوعي |

### 3. **نظام الفلترة**
- عرض جميع المنشورات المستعجلة افتراضياً
- فلترة حسب نوع الاستعجال عند الضغط على الزر
- إلغاء الفلتر والعودة لعرض الكل عند الضغط مرة أخرى

---

## 🔧 التغييرات التقنية

### 1. **نموذج البيانات (Post Model)**

#### ملف: `src/models/Post.js`

```javascript
// إضافة حقل displayPage الجديد
displayPage: {
  type: String,
  enum: ['home', 'jobs', 'haraj', 'all', 'urgent'], // ✅ إضافة 'urgent'
  default: 'home'
},

// إضافة حقل specialTag الجديد
specialTag: {
  type: String,
  enum: ['مطلوب الآن', 'عقود مؤقتة', 'دفع يومي', null],
  default: null
}
```

**التغييرات:**
- ✅ إضافة `'urgent'` إلى قيم `displayPage`
- ✅ إضافة حقل `specialTag` جديد لتصنيف نوع الاستعجال

---

### 2. **التحكم في المنشورات (Post Controller)**

#### ملف: `src/controllers/postController.js`

**أ) دالة إنشاء المنشور (`createPost`)**

```javascript
// السطر 56: إضافة specialTag إلى المتغيرات المستقبلة
const {
  // ... متغيرات أخرى
  displayPage,
  specialTag, // ✅ جديد
  isShort,
  // ...
} = req.body;

// السطر 141-142: إضافة specialTag إلى بيانات المنشور
const postData = {
  // ... بيانات أخرى
  displayPage: displayPage || 'home',
  specialTag: specialTag || null, // ✅ جديد
  isShort: isShort || false,
  // ...
};
```

**ب) دالة جلب المنشورات (`getPosts`)**

```javascript
// السطر 549: إضافة specialTag إلى معاملات الاستعلام
const {
  // ... معاملات أخرى
  displayPage,
  isShort,
  isFeatured,
  userId,
  specialTag, // ✅ جديد
  postType
} = req.query;

// السطر 582: إضافة فلترة حسب specialTag
if (specialTag) query.specialTag = specialTag;
```

**ج) دالة تحديث المنشور (`updatePost`)**

```javascript
// السطر 702: إضافة specialTag إلى الحقول المسموح تحديثها
const allowedUpdates = [
  'title', 'content', 'category', 'scope', 'country', 'city',
  'location', 'contactEmail', 'contactPhone', 'contactMethods',
  'isFeatured', 'displayPage', 'specialTag', 'price', 'currency', 'jobDetails' // ✅ specialTag
];
```

---

## 📡 API Endpoints

### 1. **جلب المنشورات المستعجلة**

```http
GET /api/v1/posts?displayPage=urgent&limit=50
Authorization: Bearer {token}
```

**الاستجابة:**
```json
{
  "success": true,
  "count": 10,
  "total": 10,
  "posts": [
    {
      "_id": "...",
      "displayPage": "urgent",
      "specialTag": "مطلوب الآن",
      "title": "...",
      "content": "...",
      "user": {...},
      // ...
    }
  ]
}
```

### 2. **فلترة حسب نوع الاستعجال**

```http
GET /api/v1/posts?displayPage=urgent&specialTag=مطلوب الآن
Authorization: Bearer {token}
```

### 3. **إنشاء منشور مستعجل**

```http
POST /api/v1/posts
Authorization: Bearer {token}
Content-Type: application/json

{
  "content": "نص المنشور",
  "displayPage": "urgent",
  "specialTag": "مطلوب الآن",
  "category": "...",
  "scope": "local",
  "country": "السعودية",
  "city": "الرياض"
}
```

---

## 🎨 الواجهة الأمامية (Frontend)

### المكونات الجديدة:

1. **`UrgentJobsView.tsx`**
   - عرض المنشورات المستعجلة
   - نظام الفلترة بالأزرار
   - واجهة مستخدم جذابة

2. **`CreatePostModal.tsx`**
   - خيار "الصفحة المستعجلة" في نطاق النشر
   - اختيار نوع الاستعجال
   - التحقق الإلزامي من اختيار النوع

### الترجمات (LanguageContext):

```javascript
// العربية
urgent_opt_now: "مطلوب الآن"
urgent_opt_temp: "عقود مؤقتة"
urgent_opt_daily: "دفع يومي"

// الإنجليزية
urgent_opt_now: "Needed Now"
urgent_opt_temp: "Temp Contracts"
urgent_opt_daily: "Daily Payment"
```

---

## 🔄 سير العمل (Workflow)

### 1. **نشر منشور مستعجل:**
```
المستخدم يفتح CreatePostModal
  ↓
يختار "الصفحة المستعجلة" في نطاق النشر
  ↓
يختار نوع الاستعجال (مطلوب الآن / عقود مؤقتة / دفع يومي)
  ↓
يكتب المحتوى ويضغط "نشر"
  ↓
يُرسل إلى الخادم: displayPage='urgent', specialTag='مطلوب الآن'
  ↓
يُحفظ في قاعدة البيانات
```

### 2. **عرض المنشورات المستعجلة:**
```
المستخدم يفتح صفحة الوظائف المستعجلة
  ↓
الواجهة تطلب: GET /api/v1/posts?displayPage=urgent
  ↓
الخادم يرجع جميع المنشورات التي displayPage='urgent'
  ↓
تُعرض جميع المنشورات (الكل)
  ↓
المستخدم يضغط على زر "مطلوب الآن"
  ↓
الواجهة تفلتر محلياً: posts.filter(p => p.specialTag === 'مطلوب الآن')
  ↓
تُعرض المنشورات المفلترة فقط
  ↓
الضغط مرة أخرى → إلغاء الفلتر → عرض الكل
```

---

## ✅ الاختبارات

### 1. **اختبار إنشاء منشور:**
```bash
curl -X POST http://localhost:5000/api/v1/posts \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "content": "مطلوب موظف فوراً",
    "displayPage": "urgent",
    "specialTag": "مطلوب الآن",
    "category": "موظف مبيعات",
    "scope": "local",
    "country": "السعودية"
  }'
```

### 2. **اختبار جلب المنشورات:**
```bash
curl -X GET "http://localhost:5000/api/v1/posts?displayPage=urgent" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 3. **اختبار الفلترة:**
```bash
curl -X GET "http://localhost:5000/api/v1/posts?displayPage=urgent&specialTag=مطلوب الآن" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 📊 قاعدة البيانات

### مثال على مستند منشور مستعجل:

```json
{
  "_id": "65a7b8c9d1e2f3a4b5c6d7e8",
  "user": "65a1b2c3d4e5f6a7b8c9d0e1",
  "title": "ابحث عن موظفين",
  "content": "مطلوب موظف مبيعات للعمل فوراً",
  "displayPage": "urgent",
  "specialTag": "مطلوب الآن",
  "category": "موظف مبيعات",
  "scope": "local",
  "country": "السعودية",
  "city": "الرياض",
  "type": "job",
  "status": "approved",
  "createdAt": "2026-01-17T10:30:00.000Z",
  "updatedAt": "2026-01-17T10:30:00.000Z"
}
```

---

## 🚀 التوافق

- ✅ **متوافق مع الإصدارات السابقة:** المنشورات القديمة لن تتأثر
- ✅ **قيمة افتراضية:** `specialTag: null` للمنشورات غير المستعجلة
- ✅ **التحقق:** يتم التحقق من اختيار `specialTag` عند اختيار `displayPage='urgent'`

---

## 📝 ملاحظات مهمة

1. **اللغة المستخدمة:** القيم المخزنة في قاعدة البيانات بالعربية فقط
2. **الفلترة:** تتم على مستوى الواجهة الأمامية للأداء الأفضل
3. **العرض الافتراضي:** عرض جميع المنشورات المستعجلة عند فتح الصفحة
4. **التبديل:** الضغط على الزر مرة أخرى يلغي الفلتر

---

## 🎉 الخلاصة

تم إضافة ميزة الوظائف المستعجلة بنجاح مع:
- ✅ نموذج بيانات محدث
- ✅ API endpoints جديدة
- ✅ واجهة مستخدم جذابة
- ✅ نظام فلترة متقدم
- ✅ توافق كامل مع الواجهة الأمامية

**الميزة جاهزة للاستخدام!** 🚀
